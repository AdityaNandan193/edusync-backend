trigger:
  branches:
    include:
    - main
  paths:
    include:
    - edusync-backend/**

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  azureSubscription: 'YOUR_AZURE_SUBSCRIPTION'
  appName: 'edusync-backend'
  resourceGroup: 'edusync-rg'
  projectPath: 'edusync-backend/edusync-backend.csproj'

steps:
- task: UseDotNet@2
  displayName: 'Install .NET SDK'
  inputs:
    packageType: 'sdk'
    version: '8.x'

- script: dotnet restore "$(projectPath)"
  displayName: 'Restore Dependencies'

- script: dotnet build "$(projectPath)" --configuration $(buildConfiguration)
  displayName: 'Build'

- script: dotnet test "$(projectPath)" --configuration $(buildConfiguration) --no-build
  displayName: 'Test'

- script: dotnet publish "$(projectPath)" --configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)
  displayName: 'Publish'

- task: AzureWebApp@1
  displayName: 'Deploy to Azure Web App'
  inputs:
    azureSubscription: '$(azureSubscription)'
    appName: '$(appName)'
    package: '$(Build.ArtifactStagingDirectory)/**/*.zip'
    deploymentMethod: 'auto' 